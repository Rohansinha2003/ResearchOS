# HuggingFace free API token (optional but recommended to avoid rate limits)
# Get yours free at: https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# Server port (optional, defaults to 5000)
PORT=5000
